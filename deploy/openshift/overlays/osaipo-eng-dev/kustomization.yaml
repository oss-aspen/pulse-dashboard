apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: osaipo-pulse

resources:
  - ../osaipo-eng

patches:
  - path: route-patch.yaml
  - path: api-route-patch.yaml

configMapGenerator:
  - name: osaipo-pulse-config
    behavior: merge
    literals:
      - ADMIN_EMAILS=
