/**
 * Static-host nav policy. Home and About are shell items (not module slugs).
 * Only these modules are listed in the sidebar / landing grid.
 */
export const STATIC_ENABLED_SLUGS = ['product-upstreams']
